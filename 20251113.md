  Backend (meet-app-be)

  1. Stworzenie enums

  - Stworzyć SportType.java - enum z typami sportów (VOLLEYBALL, BASKETBALL,
  FOOTBALL, etc.)
  - Stworzyć EventStatus.java - enum ze statusami eventów (ACTIVE, CANCELLED,
  COMPLETED, DRAFT)

  2. Aktualizacja EventEntity

  - Dodać nowe pola: sportType, status, visibilityDays, autoPromoteFromWaitlist,
  sendNotifications, group
  - Dodać adnotacje @Enumerated(EnumType.STRING) dla enumów
  - Ustawić domyślne wartości przez @Builder.Default

  3. Migracja bazy danych

  - Stworzyć V1_5__Add_missing_event_fields.sql
  - Dodać nowe kolumny do tabeli event
  - Stworzyć indeksy dla sport_type, status, start_date_time, location_id
  - Zaktualizować istniejące rekordy z domyślnymi wartościami

  4. DTOs dla tworzenia eventów

  - Stworzyć CreateEventRequest.java z walidacjami:
    - @NotBlank dla title
    - @Future dla startDateTime
    - @Min/@Max dla slots (2-100)
    - @NotNull dla wymaganych pól

  5. Rozszerzenie EventRepository

  - Dodać metody query:
    - findActiveUpcomingEvents() - aktywne przyszłe eventy
    - findEventsWithFilters() - filtrowanie po sportType, level, location, price,
  available
    - findByUserId() - eventy użytkownika
    - searchEventsByTitle() - wyszukiwanie po tytule

  6. EventService - logika biznesowa

  - Zaimplementować metodę createEvent():
    - Walidacja daty (musi być w przyszłości)
    - Obliczenie endDateTime z duration
    - Obsługa lokacji (sprawdzenie czy istnieje po współrzędnych)
    - Ustawienie initialnych wartości (slotsAvailable = slots, status = ACTIVE)
    - Przypisanie organizatora

  7. EventController - endpoint POST

  - Stworzyć endpoint POST /events:
    - Wymagana autentykacja
    - Walidacja @Valid dla request body
    - Pobranie użytkownika z Authentication
    - Zwrócenie 201 Created z utworzonym eventem

  8. Exception handling

  - Stworzyć ResourceNotFoundException.java
  - Stworzyć UnauthorizedException.java
  - Dodać handlery w GlobalExceptionHandler:
    - handleResourceNotFound() → 404
    - handleUnauthorized() → 403
    - handleIllegalArgument() → 400
    - handleIllegalState() → 409

  9. LocationRepository

  - Dodać metodę findByLatitudeAndLongitude() - zapobiega duplikatom lokacji

  Frontend (meet-app-fe)

  10. Aktualizacja EventEntity

  - Dodać nowe pola: sportType, status, visibilityDays, autoPromoteFromWaitlist,
  sendNotifications
  - Zaktualizować fromJson() i toJson()
  - Dodać do props w Equatable

  11. Enumy Flutter

  - Stworzyć sport_type.dart - enum z nazwą, displayName, ikoną emoji
  - Stworzyć event_status.dart - enum z nazwą, displayName, kolorem

  12. Event HTTP Client

  - Dodać metodę createEvent(Map<String, dynamic> eventData):
    - POST request do /api/v1/events
    - requiresAuth: true
    - Zwrócenie response z utworzonym eventem

  13. Event Service

  - Dodać metodę createEvent():
    - Wywołanie HTTP clienta
    - Obsługa statusu 201 Created
    - Error handling i logowanie
    - Zwrócenie EventEntity?

  Testowanie

  14. Testy Backend

  - Uruchomić backend i sprawdzić czy migracja się wykonała
  - Test POST /events bez autentykacji (oczekiwane 401)
  - Test POST /events z auth i poprawnymi danymi (oczekiwane 201)
  - Test POST /events z nieprawidłowymi danymi (oczekiwane 400)
  - Test POST /events z datą w przeszłości (oczekiwane 400)
  - Sprawdzić czy event się zapisał w bazie z poprawnymi wartościami

  15. Testy Frontend

  - Sprawdzić czy nowe pola są prawidłowo deserializowane z JSON
  - Sprawdzić czy enumy działają poprawnie (fromString, toJson)
  - Test wywołania createEvent() i obserwacja response

  Acceptance Criteria

  ✅ Wszystkie nowe enums stworzone✅ EventEntity zaktualizowany z nowymi polami✅
  Migracja bazy wykonana pomyślnie✅ Endpoint POST /events działa i zwraca 201✅
  Walidacja działa (daty, sloty, wymagane pola)✅ Autentykacja wymagana i
  sprawdzana✅ Lokacje nie są duplikowane (check by coordinates)✅ Error handling
  zwraca odpowiednie kody HTTP✅ Frontend deserializuje nowe pola poprawnie✅ Brak
  błędów w konsoli

  Czy chcesz żebym teraz zaczął implementację tych punktów?
